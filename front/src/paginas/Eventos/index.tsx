import { View, Text } from 'react-native'
import React from 'react'

const Eventos = () => {
  return (
    <View style={{marginTop: 60}}>
      <Text style={{textAlign: "center"}}>Eventos</Text>
    </View>
  )
}

export default Eventos;